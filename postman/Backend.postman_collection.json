{
  "info": {
    "_postman_id": "b4b6b1a0-1234-4abc-9def-tasteep-smws",
    "name": "Backend \u2014 Tasteep & SMWS Watchtower",
    "description": "Two independent projects behind one API.\n\n- **Tasteep** \u2014 spirit-tasting journal for the Flutter app. Routes `/auth/*` and `/tasteep/*`, bearer-token auth.\n- **SMWS Watchtower** \u2014 scraper + public read-only whisky feed for a website. Routes `/smws/*`, no auth on reads, optional `x-api-key` on manual scraper triggers.\n\nSet `base_url` in the environment (defaults to `http://localhost:3000`).\n\n**Getting a token:** run **Tasteep \u2014 Auth \u2192 Request Email Code**, read the 6-digit code from the server log (or your inbox if SMTP is configured), put it in `tasteep_otp_code`, then run **Verify Email Code** \u2014 the token is saved into `tasteep_token` and reused by every other Tasteep request.\n\n**Running the journal as a test suite:** the *Tastings \u2192 Atlas (phase 2) \u2192 Cleanup* folders are ordered and fully asserted. Run them with the Collection Runner, or headless with `TASTEEP_TOKEN=\u2026 npm run test:postman` (newman). Leave *Sign Out* for last \u2014 it revokes the token.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:3000"
    }
  ],
  "item": [
    {
      "name": "Tasteep \u2014 Auth",
      "item": [
        {
          "name": "Request Email Code",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('returns code_sent', () => pm.expect(body.status).to.eql('code_sent'));",
                  "console.log('Check the server log for the OTP code if SMTP is not configured, then set {{tasteep_otp_code}} and run Verify Email Code.');"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{tasteep_email}}\"\n}"
            },
            "url": {
              "raw": "{{base_url}}/auth/email",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "email"
              ]
            },
            "description": "Step 1 of email login. Sends a 6-digit code (10 min TTL). Answers 429 if a code was sent in the last 60s.\n\nIf SMTP is not configured on the server, the code is printed in the server's console log instead of emailed."
          }
        },
        {
          "name": "Verify Email Code",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('has a token', () => pm.expect(body.token).to.be.a('string'));",
                  "if (body.token) {",
                  "  pm.environment.set('tasteep_token', body.token);",
                  "  pm.environment.set('tasteep_user_id', body.id);",
                  "  console.log('Saved tasteep_token and tasteep_user_id to the environment.');",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{tasteep_email}}\",\n  \"code\": \"{{tasteep_otp_code}}\"\n}"
            },
            "url": {
              "raw": "{{base_url}}/auth/email/verify",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "email",
                "verify"
              ]
            },
            "description": "Step 2 of email login. Set the environment variable `tasteep_otp_code` to the code from the server log or your inbox, then send. On success this saves `tasteep_token` for every other Tasteep request in this collection."
          }
        },
        {
          "name": "Login with Google",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"id_token\": \"{{google_id_token}}\"\n}"
            },
            "url": {
              "raw": "{{base_url}}/auth/google",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "google"
              ]
            },
            "description": "Body carries the ID token from the client's google_sign_in flow. Requires TASTEEP_GOOGLE_CLIENT_IDS to be configured server-side; 503 otherwise."
          }
        },
        {
          "name": "Discord Authorize Redirect (browser only)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/auth/discord?redirect_uri={{discord_redirect_uri}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "discord"
              ],
              "query": [
                {
                  "key": "redirect_uri",
                  "value": "{{discord_redirect_uri}}"
                }
              ]
            },
            "description": "302 redirect to Discord's consent page. Open this in a browser, not via Send \u2014 Postman won't follow the app-deep-link redirect. Copy the `code` query param Discord appends to `discord_redirect_uri` and paste it into the `discord_code` environment variable."
          }
        },
        {
          "name": "Login with Discord",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.environment.set('tasteep_token', body.token);",
                  "  pm.environment.set('tasteep_user_id', body.id);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"code\": \"{{discord_code}}\",\n  \"redirect_uri\": \"{{discord_redirect_uri}}\"\n}"
            },
            "url": {
              "raw": "{{base_url}}/auth/discord",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "discord"
              ]
            },
            "description": "Exchanges the authorization code from the redirect step. `redirect_uri` must match what was used to open the authorize page."
          }
        },
        {
          "name": "Get Current User",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tasteep_token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "me"
              ]
            },
            "description": "Restores/validates the current session. 401 if the token is expired, unknown, or was signed out."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('is the logged-in user', () => pm.expect(body.id).to.eql(pm.environment.get('tasteep_user_id')));",
                  "pm.test('provider is known', () => pm.expect(body.provider).to.be.oneOf(['email', 'google', 'discord']));",
                  "pm.test('no token is re-issued on /me', () => pm.expect(body.token).to.be.null);"
                ]
              }
            }
          ]
        },
        {
          "name": "Sign Out",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 204', () => pm.response.to.have.status(204));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tasteep_token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/signout",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "signout"
              ]
            },
            "description": "Revokes this session server-side. The saved tasteep_token stops working immediately after this \u2014 re-run the email login requests to get a new one."
          }
        }
      ]
    },
    {
      "name": "Tasteep \u2014 Tastings",
      "description": "Phase 1 journal CRUD. Needs `tasteep_token` (run the Auth folder first, or pass it to newman). Creates two tastings (`tasting_id`, `tasting_id_2`) that the Atlas folder builds on and the Cleanup folder deletes.",
      "item": [
        {
          "name": "List Tastings",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings"
              ]
            },
            "description": "Newest date_tasted first, undated entries last. Only the caller's rows."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const TASTING_KEYS = [\"id\", \"name\", \"category\", \"photo_path\", \"distillery\", \"region\", \"region_id\", \"abv\", \"price\", \"age_statement\", \"cask_type\", \"date_tasted\", \"location\", \"score\", \"appearance\", \"tags\", \"nose\", \"palate\", \"finish\", \"lat\", \"lon\", \"location_precision\", \"created_at\", \"updated_at\"];",
                  "const expectTastingShape = (t) => pm.expect(Object.keys(t).sort()).to.eql([...TASTING_KEYS].sort());",
                  "const body = pm.response.json();",
                  "pm.test('is a plain array (no envelope)', () => pm.expect(body).to.be.an('array'));",
                  "pm.test('every item has the full Tasting contract', () => body.forEach(expectTastingShape));",
                  "pm.test('newest date_tasted first, undated last', () => {",
                  "  const dates = body.map((t) => t.date_tasted);",
                  "  const dated = dates.filter((d) => d !== null);",
                  "  for (let i = 1; i < dated.length; i++) pm.expect(dated[i - 1] >= dated[i], `${dated[i - 1]} before ${dated[i]}`).to.be.true;",
                  "  const firstNull = dates.indexOf(null);",
                  "  if (firstNull !== -1) pm.expect(dates.slice(firstNull).every((d) => d === null)).to.be.true;",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert Tasting (Create)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": "PUT is upsert. `:id` must be a UUID \u2014 the pre-request script generates `tasting_id` if the environment has none. Full replacement: any field you omit is stored as null.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Lagavulin 16\",\n  \"category\": \"whisky\",\n  \"photo_path\": null,\n  \"distillery\": \"Lagavulin\",\n  \"region\": \"Islay\",\n  \"region_id\": null,\n  \"abv\": 43,\n  \"price\": 65.5,\n  \"age_statement\": \"16\",\n  \"cask_type\": \"Ex-bourbon & sherry\",\n  \"date_tasted\": \"2026-03-10T19:00:00.000Z\",\n  \"location\": \"Home\",\n  \"score\": 88,\n  \"appearance\": \"Deep amber\",\n  \"tags\": [\n    \"peat\",\n    \"smoke\"\n  ],\n  \"nose\": \"Bonfire\",\n  \"palate\": \"Iodine\",\n  \"finish\": \"Long\",\n  \"lat\": null,\n  \"lon\": null,\n  \"location_precision\": \"unknown\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('tasting_id')) pm.environment.set('tasting_id', pm.variables.replaceIn('{{$guid}}'));"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const TASTING_KEYS = [\"id\", \"name\", \"category\", \"photo_path\", \"distillery\", \"region\", \"region_id\", \"abv\", \"price\", \"age_statement\", \"cask_type\", \"date_tasted\", \"location\", \"score\", \"appearance\", \"tags\", \"nose\", \"palate\", \"finish\", \"lat\", \"lon\", \"location_precision\", \"created_at\", \"updated_at\"];",
                  "const expectTastingShape = (t) => pm.expect(Object.keys(t).sort()).to.eql([...TASTING_KEYS].sort());",
                  "const body = pm.response.json();",
                  "pm.test('id matches the URL', () => pm.expect(body.id).to.eql(pm.environment.get('tasting_id')));",
                  "pm.test('has the full Tasting contract', () => expectTastingShape(body));",
                  "pm.test('numbers come back as numbers', () => { pm.expect(body.abv).to.eql(43); pm.expect(body.price).to.eql(65.5); pm.expect(body.score).to.eql(88); });",
                  "pm.test('dates are ISO 8601', () => pm.expect(body.date_tasted).to.eql('2026-03-10T19:00:00.000Z'));",
                  "pm.test('server owns created_at/updated_at', () => { pm.expect(body.created_at).to.be.a('string'); pm.expect(body.updated_at).to.be.a('string'); });",
                  "pm.test('starts unplaced', () => { pm.expect(body.location_precision).to.eql('unknown'); pm.expect(body.lat).to.be.null; });"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert Second Tasting (Create)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id_2}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id_2}}"
              ]
            },
            "description": "A second entry (different distillery, older date, `country` precision with coordinates) so the list order, the unplaced shelf and stats have something to show.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Ardbeg 10\",\n  \"category\": \"whisky\",\n  \"photo_path\": null,\n  \"distillery\": \"Ardbeg\",\n  \"region\": \"Islay\",\n  \"region_id\": null,\n  \"abv\": 46,\n  \"price\": 48,\n  \"age_statement\": \"10\",\n  \"cask_type\": null,\n  \"date_tasted\": \"2025-12-25T18:00:00.000Z\",\n  \"location\": \"Home\",\n  \"score\": 70,\n  \"appearance\": \"Deep amber\",\n  \"tags\": [\n    \"peat\",\n    \"citrus\"\n  ],\n  \"nose\": \"Lime & tar\",\n  \"palate\": \"Ash\",\n  \"finish\": \"Medium\",\n  \"lat\": 56.49,\n  \"lon\": -4.2,\n  \"location_precision\": \"country\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (!pm.environment.get('tasting_id_2')) pm.environment.set('tasting_id_2', pm.variables.replaceIn('{{$guid}}'));"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('id matches the URL', () => pm.expect(body.id).to.eql(pm.environment.get('tasting_id_2')));",
                  "pm.test('coordinates and precision stored', () => { pm.expect(body.lat).to.eql(56.49); pm.expect(body.lon).to.eql(-4.2); pm.expect(body.location_precision).to.eql('country'); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Get Tasting",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": "404 for an id that does not exist or belongs to another account."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const TASTING_KEYS = [\"id\", \"name\", \"category\", \"photo_path\", \"distillery\", \"region\", \"region_id\", \"abv\", \"price\", \"age_statement\", \"cask_type\", \"date_tasted\", \"location\", \"score\", \"appearance\", \"tags\", \"nose\", \"palate\", \"finish\", \"lat\", \"lon\", \"location_precision\", \"created_at\", \"updated_at\"];",
                  "const expectTastingShape = (t) => pm.expect(Object.keys(t).sort()).to.eql([...TASTING_KEYS].sort());",
                  "const body = pm.response.json();",
                  "pm.test('is the created tasting', () => { pm.expect(body.id).to.eql(pm.environment.get('tasting_id')); pm.expect(body.name).to.eql('Lagavulin 16'); });",
                  "pm.test('has the full Tasting contract', () => expectTastingShape(body));"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert Tasting (Update)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": "Replaces the same row. Fields omitted here (region, abv, price, ...) are cleared to null \u2014 this endpoint is not a PATCH.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Lagavulin 16\",\n  \"distillery\": \"Lagavulin\",\n  \"score\": 91,\n  \"tags\": [\n    \"peat\",\n    \"smoke\",\n    \"revisited\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('new values stored', () => { pm.expect(body.score).to.eql(91); pm.expect(body.tags).to.eql(['peat', 'smoke', 'revisited']); });",
                  "pm.test('omitted fields are cleared (PUT, not PATCH)', () => { pm.expect(body.region).to.be.null; pm.expect(body.abv).to.be.null; pm.expect(body.date_tasted).to.be.null; });",
                  "pm.test('defaults apply to the non-nullable fields', () => { pm.expect(body.category).to.eql('whisky'); pm.expect(body.location_precision).to.eql('unknown'); });",
                  "pm.test('updated_at bumped past created_at', () => pm.expect(body.updated_at >= body.created_at).to.be.true);"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert Tasting (Invalid \u2014 400 expected)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": "Validation: empty name, score above 100 and an unknown precision. The row is left untouched.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"\",\n  \"score\": 101,\n  \"location_precision\": \"gps\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 400', () => pm.response.to.have.status(400));",
                  "const body = pm.response.json();",
                  "pm.test('message lists every failing field', () => { pm.expect(body.message).to.be.an('array'); ['name', 'score', 'location_precision'].forEach((f) => pm.expect(body.message.join(' ')).to.include(f)); });"
                ]
              }
            }
          ]
        }
      ],
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{tasteep_token}}",
            "type": "string"
          }
        ]
      }
    },
    {
      "name": "Tasteep \u2014 Atlas (phase 2)",
      "description": "The Places backend: geocoding via Nominatim (cache-first, 1 req/s server-wide), the per-tasting location route with its manual-pin protection, the \"NOT PLACED\" shelf, and the stats aggregate. Runs after the Tastings folder; expects `tasting_id` and `tasting_id_2` to exist.",
      "item": [
        {
          "name": "List Unplaced Tastings",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings?unplaced=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings"
              ],
              "query": [
                {
                  "key": "unplaced",
                  "value": "true"
                }
              ]
            },
            "description": "Only `location_precision` in `unknown`/`country` \u2014 the Atlas \"NOT PLACED\" shelf (`unknown` \u2192 SET LOCATION, `country` \u2192 REFINE). Both tastings created above qualify at this point."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('only unknown/country precision', () => body.forEach((t) => pm.expect(t.location_precision).to.be.oneOf(['unknown', 'country'])));",
                  "const ids = body.map((t) => t.id);",
                  "pm.test('both fresh tastings are on the shelf', () => { pm.expect(ids).to.include(pm.environment.get('tasting_id')); pm.expect(ids).to.include(pm.environment.get('tasting_id_2')); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Geocode a Place",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/geocode",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "geocode"
              ]
            },
            "description": "Cache-first Nominatim lookup. The first ever run of a query hits Nominatim (max 1 req/s for the whole server); every later run of the same normalised query is served from `tasteep_geocode_cache`. 503 if `TASTEEP_NOMINATIM_USER_AGENT` is not configured server-side. Saves the result into `geocode_*` for the next requests.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"Islay, Scotland\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('returns {lat, lon, precision}', () => { pm.expect(Object.keys(body).sort()).to.eql(['lat', 'lon', 'precision']); pm.expect(body.lat).to.be.a('number'); pm.expect(body.lon).to.be.a('number'); });",
                  "pm.test('coordinates are in range', () => { pm.expect(body.lat).to.be.within(-90, 90); pm.expect(body.lon).to.be.within(-180, 180); });",
                  "pm.test('precision is an automated value', () => pm.expect(body.precision).to.be.oneOf(['exact', 'region', 'country']));",
                  "pm.test('Islay lands in the Hebrides', () => { pm.expect(body.lat).to.be.within(55.5, 56.0); pm.expect(body.lon).to.be.within(-6.6, -5.9); });",
                  "pm.environment.set('geocode_lat', body.lat);",
                  "pm.environment.set('geocode_lon', body.lon);",
                  "pm.environment.set('geocode_precision', body.precision);"
                ]
              }
            }
          ]
        },
        {
          "name": "Geocode a Place (cache hit)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/geocode",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "geocode"
              ]
            },
            "description": "Same place, different casing and whitespace: normalised to the same cache key, so this never reaches Nominatim.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"  ISLAY,   Scotland \"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('identical to the first result', () => { pm.expect(body.lat).to.eql(pm.environment.get('geocode_lat')); pm.expect(body.lon).to.eql(pm.environment.get('geocode_lon')); pm.expect(body.precision).to.eql(pm.environment.get('geocode_precision')); });",
                  "pm.test('served fast (cache, no upstream call)', () => pm.expect(pm.response.responseTime).to.be.below(500));"
                ]
              }
            }
          ]
        },
        {
          "name": "Geocode Unknown Place (404 expected)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/geocode",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "geocode"
              ]
            },
            "description": "A miss is cached as `unknown` too, so the same query never hits Nominatim again.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"xq zv nowhere-at-all tasteep\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 404', () => pm.response.to.have.status(404));"
                ]
              }
            }
          ]
        },
        {
          "name": "Geocode Empty Query (400 expected)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/geocode",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "geocode"
              ]
            },
            "description": "Validation only \u2014 Nominatim is not called.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"   \"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 400', () => pm.response.to.have.status(400));"
                ]
              }
            }
          ]
        },
        {
          "name": "Apply Geocode Result to Tasting",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "What the client does after a geocode: store the result on the tasting. The tasting is `unknown` at this point, so an automated precision is accepted. The body is built from the `geocode_*` variables saved by \"Geocode a Place\" (raw JSON, numbers unquoted \u2014 see the pre-request script).",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lat\": \"{{geocode_lat}}\",\n  \"lon\": \"{{geocode_lon}}\",\n  \"precision\": \"{{geocode_precision}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Postman variables are strings inside a raw body; rebuild the JSON so lat/lon stay numbers.",
                  "pm.request.body.raw = JSON.stringify({",
                  "  lat: Number(pm.environment.get('geocode_lat')),",
                  "  lon: Number(pm.environment.get('geocode_lon')),",
                  "  precision: pm.environment.get('geocode_precision'),",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const TASTING_KEYS = [\"id\", \"name\", \"category\", \"photo_path\", \"distillery\", \"region\", \"region_id\", \"abv\", \"price\", \"age_statement\", \"cask_type\", \"date_tasted\", \"location\", \"score\", \"appearance\", \"tags\", \"nose\", \"palate\", \"finish\", \"lat\", \"lon\", \"location_precision\", \"created_at\", \"updated_at\"];",
                  "const expectTastingShape = (t) => pm.expect(Object.keys(t).sort()).to.eql([...TASTING_KEYS].sort());",
                  "const body = pm.response.json();",
                  "pm.test('returns the full updated tasting', () => expectTastingShape(body));",
                  "pm.test('geocode result stored', () => { pm.expect(body.lat).to.eql(pm.environment.get('geocode_lat')); pm.expect(body.lon).to.eql(pm.environment.get('geocode_lon')); pm.expect(body.location_precision).to.eql(pm.environment.get('geocode_precision')); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Set Manual Location Pin",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "A pin drop. Always accepted, and future automated geocode results will not overwrite it (see next requests).",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lat\": 55.63,\n  \"lon\": -6.13,\n  \"precision\": \"manual\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('pin stored as manual', () => { pm.expect(body.lat).to.eql(55.63); pm.expect(body.lon).to.eql(-6.13); pm.expect(body.location_precision).to.eql('manual'); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Set Automated Location (rejected \u2014 409 expected)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "The manual-pin protection, enforced server-side: an automated result never overwrites a manual pin.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lat\": 1,\n  \"lon\": 2,\n  \"precision\": \"exact\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('rejected because the tasting has a manual pin', () => pm.response.to.have.status(409));",
                  "pm.test('explains why', () => pm.expect(pm.response.json().message).to.include('pinned manually'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert Keeps Manual Pin",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": "The same protection on the full upsert: a body carrying an automated precision does not move a manual pin. (Sending `manual` with new coordinates would move it.)",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Lagavulin 16\",\n  \"category\": \"whisky\",\n  \"photo_path\": null,\n  \"distillery\": \"Lagavulin\",\n  \"region\": \"Islay\",\n  \"abv\": 43,\n  \"price\": 65.5,\n  \"age_statement\": \"16\",\n  \"cask_type\": \"Ex-bourbon & sherry\",\n  \"date_tasted\": \"2026-03-10T19:00:00.000Z\",\n  \"location\": \"Home\",\n  \"score\": 88,\n  \"appearance\": \"Deep amber\",\n  \"tags\": [\n    \"peat\",\n    \"smoke\"\n  ],\n  \"nose\": \"Bonfire\",\n  \"palate\": \"Iodine\",\n  \"finish\": \"Long\",\n  \"lat\": 1,\n  \"lon\": 2,\n  \"location_precision\": \"exact\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('manual pin survived the upsert', () => { pm.expect(body.lat).to.eql(55.63); pm.expect(body.lon).to.eql(-6.13); pm.expect(body.location_precision).to.eql('manual'); });",
                  "pm.test('the rest of the body was applied', () => pm.expect(body.region).to.eql('Islay'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Move Manual Pin",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "A second manual pin replaces the first.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lat\": 55.64,\n  \"lon\": -6.12,\n  \"precision\": \"manual\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('pin moved', () => { pm.expect(body.lat).to.eql(55.64); pm.expect(body.lon).to.eql(-6.12); pm.expect(body.location_precision).to.eql('manual'); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Location Without Coordinates (400 expected)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "lat/lon are required for every precision except `unknown`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"precision\": \"manual\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 400', () => pm.response.to.have.status(400));"
                ]
              }
            }
          ]
        },
        {
          "name": "Clear Location",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "Clears lat/lon even over a manual pin \u2014 this is the one precision value that always wins. The tasting is back on the \"NOT PLACED\" shelf.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"precision\": \"unknown\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('coordinates cleared', () => { pm.expect(body.lat).to.be.null; pm.expect(body.lon).to.be.null; pm.expect(body.location_precision).to.eql('unknown'); });"
                ]
              }
            }
          ]
        },
        {
          "name": "Set Automated Location (accepted after clear)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}/location",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}",
                "location"
              ]
            },
            "description": "Once the pin is cleared, automated results are accepted again.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lat\": 55.7585,\n  \"lon\": -6.1657,\n  \"precision\": \"region\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "pm.test('automated result stored', () => pm.expect(pm.response.json().location_precision).to.eql('region'));"
                ]
              }
            }
          ]
        },
        {
          "name": "List Unplaced Tastings (after placing)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings?unplaced=true",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings"
              ],
              "query": [
                {
                  "key": "unplaced",
                  "value": "true"
                }
              ]
            },
            "description": "`tasting_id` is now `region`, so it left the shelf; `tasting_id_2` (`country`) is still there."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const ids = pm.response.json().map((t) => t.id);",
                  "pm.test('placed tasting left the shelf', () => pm.expect(ids).to.not.include(pm.environment.get('tasting_id')));",
                  "pm.test('country-precision tasting is still on it', () => pm.expect(ids).to.include(pm.environment.get('tasting_id_2')));"
                ]
              }
            }
          ]
        },
        {
          "name": "List Regions",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/regions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "regions"
              ]
            },
            "description": "The curated region picker: countries in seed order, each with `subregions` (may be empty). Every row carries an approximate centroid `lat`/`lon` \u2014 the Atlas uses it as the fallback pin for a tasting that has no resolved location. Send the returned `ETag` back as `If-None-Match` to get a bodyless `304`. Stores the Islay id in `region_id` for the next request."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const tree = pm.response.json();",
                  "pm.test('is a non-empty array', () => { pm.expect(tree).to.be.an('array'); pm.expect(tree.length).to.be.at.least(30); });",
                  "pm.test('country rows have the exact shape', () => pm.expect(Object.keys(tree[0]).sort()).to.eql(['id', 'lat', 'lon', 'name', 'subregions']));",
                  "const scotland = tree.find((r) => r.id === 'scotland');",
                  "pm.test('Scotland is there with its whisky regions', () => { pm.expect(scotland.name).to.eql('Scotland'); pm.expect(scotland.subregions.map((s) => s.id)).to.include.members(['scotland/speyside', 'scotland/islay', 'scotland/campbeltown']); });",
                  "const islay = scotland.subregions.find((s) => s.id === 'scotland/islay');",
                  "pm.test('subregion rows have the exact shape and a centroid', () => { pm.expect(Object.keys(islay).sort()).to.eql(['id', 'lat', 'lon', 'name']); pm.expect(islay.lat).to.be.a('number'); pm.expect(islay.lon).to.be.a('number'); });",
                  "pm.test('a country without subregions has an empty list', () => pm.expect(tree.find((r) => r.id === 'sweden').subregions).to.eql([]));",
                  "pm.test('carries a weak ETag', () => pm.expect(pm.response.headers.get('ETag')).to.match(/^W\\/\"[0-9a-f]{40}\"$/));",
                  "pm.environment.set('region_id', islay.id);"
                ]
              }
            }
          ]
        },
        {
          "name": "List Regions (304 on If-None-Match)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "If-None-Match",
                "value": "{{regions_etag}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/regions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "regions"
              ]
            },
            "description": "Same call with the ETag from the previous request; expects `304` and no body."
          },
          "response": [],
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Newman runs requests in order; grab the ETag with a direct call so this request is self-contained.",
                  "pm.sendRequest({ url: pm.variables.replaceIn('{{base_url}}/tasteep/regions'), method: 'GET', header: { Authorization: 'Bearer ' + pm.environment.get('tasteep_token') } }, (err, res) => { if (!err) pm.environment.set('regions_etag', res.headers.get('ETag')); });"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 304', () => pm.response.to.have.status(304));",
                  "pm.test('no body', () => pm.expect(pm.response.text()).to.eql(''));"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert With Picked Region",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id_2}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id_2}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Ardbeg 10\",\n  \"category\": \"whisky\",\n  \"photo_path\": null,\n  \"distillery\": \"Ardbeg\",\n  \"region\": null,\n  \"region_id\": \"{{region_id}}\",\n  \"abv\": 46,\n  \"price\": 48,\n  \"age_statement\": \"10\",\n  \"cask_type\": null,\n  \"date_tasted\": \"2025-12-25T18:00:00.000Z\",\n  \"location\": null,\n  \"score\": 70,\n  \"appearance\": null,\n  \"tags\": [\n    \"peat\"\n  ],\n  \"nose\": null,\n  \"palate\": null,\n  \"finish\": null,\n  \"lat\": null,\n  \"lon\": null,\n  \"location_precision\": \"unknown\"\n}"
            },
            "description": "`region_id` from the picker wins and, with `region` left null, the server fills the text with the region's name. An unknown `region_id` is a `400`. Free text that matches a known name (e.g. `\"Islay\"`) is linked automatically; anything else is kept as typed with `region_id: null`."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('region_id stored', () => pm.expect(body.region_id).to.eql(pm.environment.get('region_id')));",
                  "pm.test('region text filled from the picker', () => pm.expect(body.region).to.eql('Islay'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Upsert With Unknown region_id (400 expected)",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id_2}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id_2}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Ardbeg 10\",\n  \"region_id\": \"atlantis\"\n}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 400', () => pm.response.to.have.status(400));",
                  "pm.test('names the field', () => pm.expect(JSON.stringify(pm.response.json().message)).to.include('region_id'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Stats",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/stats",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "stats"
              ]
            },
            "description": "`{count, avg_score, distinct_distilleries}` for the Profile stat grid. `avg_score` is null when no scored tasting exists."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('exact shape', () => pm.expect(Object.keys(body).sort()).to.eql(['avg_score', 'count', 'distinct_distilleries']));",
                  "pm.test('counts both fresh tastings', () => pm.expect(body.count).to.be.at.least(2));",
                  "pm.test('avg_score is a 1-decimal number between 0 and 100', () => { pm.expect(body.avg_score).to.be.a('number'); pm.expect(body.avg_score).to.be.within(0, 100); pm.expect(Math.round(body.avg_score * 10) / 10).to.eql(body.avg_score); });",
                  "pm.test('at least Lagavulin and Ardbeg', () => pm.expect(body.distinct_distilleries).to.be.at.least(2));"
                ]
              }
            }
          ]
        }
      ],
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{tasteep_token}}",
            "type": "string"
          }
        ]
      }
    },
    {
      "name": "Tasteep \u2014 Cleanup",
      "description": "Removes the two tastings the run created so it can be repeated. Sign Out lives in the Auth folder on purpose: it revokes `tasteep_token`, so run it only when you are done.",
      "item": [
        {
          "name": "Delete Tasting",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": "204 on success, 404 if already deleted, never created, or owned by someone else."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 204', () => pm.response.to.have.status(204));"
                ]
              }
            }
          ]
        },
        {
          "name": "Delete Second Tasting",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id_2}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id_2}}"
              ]
            },
            "description": ""
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 204', () => pm.response.to.have.status(204));"
                ]
              }
            }
          ]
        },
        {
          "name": "Get Deleted Tasting (404 expected)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": ""
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 404', () => pm.response.to.have.status(404));"
                ]
              }
            }
          ]
        },
        {
          "name": "Delete Again (404 expected)",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{base_url}}/tasteep/tastings/{{tasting_id}}",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "tasteep",
                "tastings",
                "{{tasting_id}}"
              ]
            },
            "description": ""
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('status is 404', () => pm.response.to.have.status(404));"
                ]
              }
            }
          ]
        }
      ],
      "auth": {
        "type": "bearer",
        "bearer": [
          {
            "key": "token",
            "value": "{{tasteep_token}}",
            "type": "string"
          }
        ]
      }
    },
    {
      "name": "SMWS \u2014 Watchtower (public)",
      "item": [
        {
          "name": "Live Inventory",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/smws/watchtower/live",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "smws",
                "watchtower",
                "live"
              ]
            },
            "description": "No auth. Bottles currently listed on smws.eu, newest first."
          }
        },
        {
          "name": "Archive Inventory",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/smws/watchtower/archive",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "smws",
                "watchtower",
                "archive"
              ]
            },
            "description": "No auth. Every bottle the archive scraper has ever seen, newest first."
          }
        }
      ]
    },
    {
      "name": "SMWS \u2014 Scraper (x-api-key)",
      "item": [
        {
          "name": "Run Live Scraper",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "x-api-key",
                "value": "{{scraper_api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/smws/scraper/run-live",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "smws",
                "scraper",
                "run-live"
              ]
            },
            "description": "Triggers the live scraper immediately. Requires SMWS_SCRAPER_API_KEY to be set server-side and matched here \u2014 403 otherwise."
          }
        },
        {
          "name": "Run Archive Scraper",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "x-api-key",
                "value": "{{scraper_api_key}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/smws/scraper/run-archive",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "smws",
                "scraper",
                "run-archive"
              ]
            },
            "description": "Triggers the archive scraper immediately. Same auth as run-live."
          }
        }
      ]
    }
  ]
}
